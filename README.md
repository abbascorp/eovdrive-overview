# eovdrive-overview
EOVDrive is a pre-revenue C++ technical asset built around a compressed and verifiable virtual volume format called `.eov`.

The project vision is to make storage smarter: instead of storing files in one fixed way, EOVDrive breaks data into chunks, evaluates compression strategies, stores the best valid representation, verifies integrity, and allows data to be read back reliably from the compressed volume.

## What Works

- C++ core engine builds
- CLI can create `.eov` volumes
- Import/export/list/stats/verify/read-range flows work
- Chunk/manifest/index-based volume structure
- Multiple codec implementations
- Local validation and smoke tests included

## Included in the Sale

- Full C++ source code
- CMake build system
- CLI tool
- codec implementations
- validation scripts and tests
- documentation
- sample `.eov` volumes
- Windows service, installer, ProjFS/native driver prototype code

## Current Status

This is not being sold as a finished consumer product.

The strongest part is the read-only compressed volume/archive engine and CLI.  
The native Windows driver/mount layer is experimental and not production-ready.  
Write support is not production-ready.

## Best Buyer

A C++/Windows/storage/compression developer or small team interested in compression, archive formats, virtual filesystems, or developer utilities.

## Asking Price

$750 OBO. Fast-close offers around $500-$600 may be considered.

## Sale Package

The full source package, tests, docs, and handoff materials are provided after payment/escrow.
